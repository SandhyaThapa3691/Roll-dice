import 'package:flutter/material.dart';

class Rolldice extends StatelessWidget {
  final String dice;
  const Rolldice({super.key, required this.dice});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}